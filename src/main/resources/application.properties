# Configuración de la base de datos MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/gestor
spring.datasource.username=root
spring.datasource.password=gonzalo1
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect

# Configuración del puerto del servidor web (por defecto es 8080)
server.port=8080

# Configuración de logging
logging.level.org.springframework=INFO
logging.level.com.yourpackage=DEBUG
#  justa esto para tu propio paquete

# Otros ajustes de aplicación

